{
  "name": "opusclaw",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "OpusClaw — AI Gateway & Chat Platform with voice mode, multi-provider support, and channel integrations",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test test/**/*.test.js",
    "lint": "node -c src/server.js"
  },
  "dependencies": {
    "@slack/bolt": "^4.0.0",
    "better-sqlite3": "^11.0.0",
    "discord.js": "^14.0.0",
    "express": "^5.1.0",
    "telegraf": "^4.0.0",
    "ws": "^8.19.0"
  }
}
